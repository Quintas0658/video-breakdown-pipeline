# 🎨 Excalidraw 可视化笔记功能规划

- **状态**: 🔬 调研中
- **创建日期**: 2026-02-16
- **灵感来源**: [excalidraw-mcp](https://github.com/excalidraw/excalidraw-mcp) — Excalidraw 官方 MCP 服务器

---

## 核心想法

在 video-breakdown-pipeline 的输出中增加 **手绘风格的可视化笔记**，让用户不仅能看到文字拆解，还能看到结构化的视觉笔记。

最终愿景：**像有人在实时做笔记一样的动态演示视频**。

---

## 迭代路径

### Phase 1: MVP — 静态手绘笔记图 ✏️

> 目标: 让 pipeline 输出一张手绘风格的笔记图

- 在 Step 2 拆解完成后，用 AI 生成 Excalidraw 场景 JSON
- 将 JSON 导出为 PNG/SVG 静态图片
- 笔记内容：视频核心观点 + 关键步骤 + 行动项
- **技术选择**:
  - 方案 A: 用 `@excalidraw/excalidraw` 的 `exportToSvg/exportToBlob` API (需 Node.js)
  - 方案 B: 调 Excalidraw 的在线导出服务
  - 方案 C: 直接生成 `.excalidraw` JSON 文件，用户手动打开查看

### Phase 2: 动态笔记演示 🎬

> 目标: 生成"像人在实时写笔记"的动画效果

- 用 `@excalidraw/excalidraw` React 组件搭建一个轻量前端
- 实现逐步添加元素 + 视口平移的动画逻辑
- 用户可以：
  - 在浏览器中观看笔记动画
  - 导出为视频（用于社交媒体分享）
- **参考**: excalidraw-mcp 的 streaming + viewport camera control 实现

---

## 调研笔记

### excalidraw-mcp 调研 (2026-02-16)

**它做了什么？**
- Excalidraw 官方 MCP 服务器
- 让 AI (Claude/ChatGPT) 在聊天中实时生成手绘图表
- "动画效果" = 流式渲染 + 视口摄像机控制

**对我们的启发：**
- MCP 方案不适合直接集成到 Python pipeline，它需要 AI 聊天客户端
- 但它的视觉效果值得复刻：逐步出现 + 丝滑平移
- `@excalidraw/excalidraw` npm 库可以直接在前端使用，自己实现类似效果

**关键资源：**
- GitHub: https://github.com/excalidraw/excalidraw-mcp
- npm 库: https://www.npmjs.com/package/@excalidraw/excalidraw
- Excalidraw JSON 格式: https://github.com/excalidraw/excalidraw/blob/master/packages/excalidraw/data/restore.ts
